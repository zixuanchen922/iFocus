# iFocus 项目重要设定

本文档记录后续开发必须保持一致的核心设定。参数发生正式调整后，应同步更新本文档与代码，避免动画之间出现坐标、尺寸或视觉状态无法衔接的问题。

## 1. 小球的统一模型

### 1.1 定位基准

- 所有动画始终使用**完整小球的中心坐标**定位。
- 不使用眼睛中心、可见区域中心或裁切后图形中心作为小球的位置参考。
- 即使小球被放大到画面外、最终只看得到眼睛，小球仍然作为一个完整圆形模型存在。
- 小球中心允许位于画布外；画面裁切只是显示结果，不改变模型坐标。

这保证待机动作、触发动作和最终特写能够共享同一套位置参数，不会因为切换参考系产生跳动。

### 1.2 逻辑画布

- 横屏基准画布：`1000 × 460`。
- 竖屏初始页画布：`460 × 1000`，是横屏画布顺时针旋转后的对应方向。
- 逻辑坐标原点位于画布左上角。
- `x` 向右增加，`y` 向下增加。
- 网页根据设备尺寸对整张逻辑画布做等比缩放，并在多余方向居中留白。

适配比例：

```text
横屏 canvasScale = min(实际宽度 / 1000, 实际高度 / 460)
竖屏 canvasScale = min(实际宽度 / 460, 实际高度 / 1000)
```

留白偏移：

```text
offsetX = (实际宽度 - 逻辑画布宽度 × canvasScale) / 2
offsetY = (实际高度 - 逻辑画布高度 × canvasScale) / 2
```

设备上的像素坐标必须先扣除画布偏移，再除以 `canvasScale`，才能转换为统一的逻辑坐标。因此不同手机或电脑虽然像素尺寸不同，动画参数仍使用同一套定义。

### 1.3 尺寸基准

- 初始小球直径定义为 `120` 个逻辑单位。
- 初始小球大小定义为 `100%`，对应 `scale = 1`。
- 后续所有尺寸变化只使用相对于初始大小的缩放倍数表达。
- 不为特写状态另建一套小球尺寸。

示例：

```text
scale = 1    → 初始大小 100%
scale = 8.7  → 初始大小的 8.7 倍
```

## 2. 小球姿态数据

代码中的小球姿态由以下数据构成：

```ts
interface OrbPose {
  center: { x: number; y: number };
  scale: number;
  eyeHalfSpacing: number;
}
```

- `center`：完整小球在逻辑画布中的中心坐标，是唯一的位置基准。
- `scale`：相对于初始小球 `100%` 的缩放倍数。
- `eyeHalfSpacing`：单只眼睛中心到双眼中线的距离，只用于避免切换模型时眼距跳变，不参与小球定位。

### 2.1 页面基准姿态

横屏作为统一标定方向；竖屏初始页使用顺时针旋转后的对应画布和坐标：

```text
竖屏初始页（460 × 1000）：center = (230, 235)，scale = 1
横屏专注页（1000 × 460）：center = (180, 260)，scale = 1.2
```

- 横屏专注页与竖屏初始页分别保存独立位置参数；屏幕适配仍使用各自固定逻辑画布同比例缩放。
- START 直接在这两个完整小球姿态之间插值。
- 不再使用隐藏 DOM 探针定义专注页终点。
- `starting` 阶段直接使用最终专注画布；时钟只挂载和播放一次，切换到 `focused` 时不得重新初始化。
- START 完成进入 `focused` 时为静默状态切换，不重复播放提示音。
- 页面旋转只改变逻辑坐标到设备像素的映射，不改变上述中心参数。
- 顶层小球不使用会改变投影中心的 Z 轴位移；球体内部形变和旋转仍可独立播放。

## 3. 触发动画的动态起点

触发动作不能假设小球总从固定位置出发，因为待机动作会改变小球的位置。

触发 `001` 时，程序应在切换显示状态之前读取当前 DOM 中：

- 小球实际边界框；
- 小球中心；
- 当前小球缩放；
- 左右眼的实际中心间距。

读取到的设备像素数据转换到 `1000 × 460` 逻辑画布后，作为本次动画的 `startPose`。动画不读取或推算小球速度，只读取触发瞬间的姿态。

如果读取失败，当前备用起点为：

```text
center = (180, 260)
scale = 1.2
eyeHalfSpacing = 21
```

备用参数不是正常动画的固定出发点，只用于 DOM 不可用时防止动画失效。

## 4. 001 的固定终点

当前确认的一级提醒终点参数：

```text
完整小球中心：center = (500, 230)
完整小球缩放：scale = 8.7
最终单眼半间距：eyeHalfSpacing = 10.25
```

该状态仍然渲染完整小球。因为小球被放大，画面中主要看到双眼，同时可以看到部分白色外圈。

调整最终构图时，只优先调整以下两个参数：

1. `endCenter`：控制完整小球中心位置；
2. `endScale`：控制完整小球整体缩放。

不要直接移动最终双眼来补偿构图，否则会再次引入眼睛坐标系。

## 5. 001 的位置与缩放变化

设触发时姿态为 `startPose`，固定终点为 `endPose`。位置始终通过起点和终点之间的相对进度计算：

```text
current = start + (end - start) × progress
```

因此无论小球从待机画面的哪个位置触发，都能进入同一个最终状态。

### 5.1 横向位置进度

```text
0%   → 0
10%  → 0.40625
25%  → 1
100% → 1
```

横向位置在前 `25%` 动画内进入最终坐标。

### 5.2 纵向位置进度

```text
0%   → 0
10%  → 0.25
25%  → 0.625
48%  → 0.875
64%  → 1
100% → 1
```

纵向位置在 `64%` 节点到达最终坐标。

### 5.3 缩放与回弹

主缩放阶段：

```text
0%  → 起始缩放
10% → 完成总缩放差的 4.9%
25% → 完成总缩放差的 43.7%
48% → 完成总缩放差的 83.2%
```

随后进入靠近最终尺寸的轻微回弹：

```text
64%  → endScale × 0.9943
76%  → endScale × 1.0092
88%  → endScale × 0.9908
100% → endScale
```

回弹围绕固定的 `endScale` 计算，因此不会因为起始大小不同而改变最终尺寸。

### 5.4 眼距衔接

- 首帧使用触发瞬间读取的真实 `eyeHalfSpacing`，防止双眼在模型切换时突然靠拢。
- 随横向运动进度平滑过渡到最终值 `10.25`。
- 眼距变化只属于小球内部表情布局，不改变完整小球中心坐标。

## 6. 时间与眨眼

- 001 的位移、缩放、眼距过渡和回弹按接口持续时间的 `1.2×` 速度播放。
- 接口中的提醒持续时间不变；主体动画提前完成后停留在最终状态。
- 眨眼从主体动画 `64%` 节点开始，也就是小球到达最终位置、准备进入明显回弹的时刻。
- 眨眼节奏：快速眨两下，停 `0.4s`，眨一下，停 `0.7s`，再快速眨两下。
- 单次眨眼持续约 `140ms`，通过 SVG 眼睛纵向连续缩放实现，不能替换成低分辨率位图。

当前五次眨眼的相对开始时间为：

```text
0ms, 190ms, 730ms, 1570ms, 1760ms
```

## 7. 提醒保持与 111 恢复

- `001`、`002`、`003` 播放完成后保持在各自最终帧，不再根据 `duration` 自动切回专注状态。
- `display: 111` 是提醒状态返回正常专注状态的唯一恢复指令。
- 提醒保持或 111 回程期间收到 `display: 000` 时只记录消息，不改变当前画面。
- 111 回程从统一最终姿态出发，回到横屏初始姿态 `center = (180, 260)`、`scale = 1.2`。
- 回程复用 001 主运动关键帧的反向顺序，不包含最终回弹，也不播放眨眼。
- 回程完成前隐藏普通小球和时钟；完成后再切换为 `focused`，避免模型重叠或中途跳帧。

## 8. 后续动画的开发约束

- 新待机动作可以自由改变小球位置，但必须保留可读取的完整小球 DOM 边界。
- 所有提醒动画都应从触发瞬间的实际姿态出发，不能回到写死的起点后再播放。
- 关键帧应保存相对进度，不保存只适用于某个起点的绝对中间坐标。
- 最终状态由完整小球的 `center + scale` 定义；眼睛形状和眼距是模型内部属性。
- 002、003 即使需要改变眼睛形状，也应继续复用相同的小球中心与缩放坐标系。
- 泛光应与清晰的 SVG 核心图形分层渲染：核心保持锐利，独立副本负责模糊发光。

## 9. 当前代码位置

上述定义当前主要位于：

- `src/App.tsx`
  - `PRIMARY_ORB_MODEL`
  - `OrbPose`
  - `readCurrentOrbPose`
  - `PrimaryReminderModel`
  - `PrimaryRecoveryModel`
  - `primaryBlinkScale`

更新这些实现时，应同时核对本文档中的参数和原则。
